{
  "name": "flip-finder-ai",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/ai.js && node --check src/db.js && node --check src/logic.js && node --test"
  },
  "devDependencies": {
    "@netlify/functions": "^5.3.0"
  }
}
